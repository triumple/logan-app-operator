sudo: required

language: go

os:
  - linux
#  - osx

#branches:
#  only:
#    - master
#    - develop

go:
  - "1.12"

services:
  - docker

go_import_path: github.com/logancloud/logan-app-operator

before_install:
  - wget https://github.com/operator-framework/operator-sdk/releases/download/v0.8.1/operator-sdk-v0.8.1-x86_64-linux-gnu
  - mkdir -p $HOME/bin
  - mv operator-sdk-v0.8.1-x86_64-linux-gnu $HOME/bin/operator-sdk
  - chmod +x $HOME/bin/operator-sdk
  - export PATH="$HOME/bin:$PATH"

install:
  - go get -u github.com/golang/dep/cmd/dep
  - go get -u github.com/onsi/ginkgo/ginkgo

script:
  - make docker-build
  - if [[ ${TRAVIS_REPO_SLUG} != "logancloud/logan-app-operator" ]]; then
      echo "fuck 1"
      webhooks = "https://oapi.dingtalk.com/robot/send?access_token=d97c04116fd741faa4d441a1fe474d5e9ce8ea621cb88ba3d7e48cd250a3a604"
    else
      echo "fuck 2"
      webhooks = ""
    fi
  - make travis-build

after_script:
  - docker logout

notifications:
  email: false

  #webhooks: https://oapi.dingtalk.com/robot/send?access_token=d97c04116fd741faa4d441a1fe474d5e9ce8ea621cb88ba3d7e48cd250a3a604