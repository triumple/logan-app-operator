java:
  settings:
    registry: "registry.logan.local"
  oEnvs:
    app:
      test:
        env:
          # Podpreset
          - name: SPRING_ZIPKIN_ENABLED
            value: "false"
          - name: MY_OENV_APP
            value: "B"
  app:
    env:
      # Podpreset
      - name: SPRING_ZIPKIN_ENABLED
        value: "true"
      - name: MY_ENV_APP
        value: "A"